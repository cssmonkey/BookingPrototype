app.controller('availabilityController', function ($scope, $rootScope, bookFormService) {

    $rootScope.pageLoading = false; // page content loaded

    // local scope
    $scope.availability = {};


});